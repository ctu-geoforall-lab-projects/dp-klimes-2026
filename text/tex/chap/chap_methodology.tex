\chapter{Data and Methodology} \label{chap:methodology}


% na transfer shadow class bude potřeba partial freezing - freeze jedné výstupní classy - asi se to nazývá Constrained Fine-tuning?

The objective of this thesis is to investigate the applicability of transfer learning techniques for semantic segmentation of clouds in multispectral satellite imagery under cross-sensor conditions. The study focuses on transferring knowledge from Sentinel-2 
(S2) imagery to the VEN$\mu$S imagery, which differs in spatial resolution, spectral configuration, and radiometric properties.

A secondary objective is to evaluate whether information related to the cloud-shadow class, learned from Sentinel-2 data, can be preserved during adaptation to the target domain, despite the absence of cloud-shadow annotations in the VEN$\mu$S dataset.

Several experimental scenarios are considered, including direct application of a source-trained model to target data, input-level radiometric normalization, and fine-tuning-based strategies.

All experiments are based on a convolutional neural network architecture called U-Net. The model is first trained on Sentinel-2 data and subsequently adapted to VEN$\mu$S data under different transfer learning settings.

This chapter describes the datasets, preprocessing pipeline, model architecture, training procedure, transfer learning strategies, and evaluation methodology used in this study.

\section{Datasets}
\label{sec:datasets}

In this study, two datasets are used, each acquired by a different satellite sensor. The first consists of Sentinel-2 imagery from the CloudSEN12+ dataset, selected from several available labeled Sentinel-2 cloud detection datasets, including KappaSet \cite{kappaset2022}, the Sentinel-2 Cloud Mask Catalogue \cite{s2cloudmask}, a cloud mask dataset developed by Hollstein et al.\ \cite{hollstein2016}, dataset created by Baetens and Hagolle \cite{baetens2018}, and a dataset by Luo et al. \cite{luo2024}. CloudSEN12+ \cite{aybar2024} was chosen as it is the largest publicly available cloud detection dataset for Sentinel-2 imagery with high-quality expert-reviewed annotations and provided the most extensive patch coverage over the target region among the evaluated options. The second dataset consists of VEN$\mu$S imagery from a dedicated VEN$\mu$S cloud masking dataset created by Pešek et al. \cite{pesek2022}. Both sensors operate in the visible and near-infrared spectral range and share a sufficient number of spectrally equivalent bands to enable cross-sensor transfer learning, as described in Section~\ref{sec:band_selection}. The two datasets differ in spatial resolution, spectral configuration, and available annotation classes, making this a characteristic example of a cross-sensor transfer learning scenario.

An important distinction between the datasets lies in the annotation classes that are available. CloudSEN12+ provides four semantic classes: thick cloud, thin cloud, cloud shadow, and clear. The VEN$\mu$S dataset provides three classes: thick cloud, thin cloud, and clear, with no cloud shadow annotations. The treatment of the cloud shadow class in the context of cross-sensor transfer is discussed in Section~\ref{}.

\subsection{Sentinel-2 and CloudSEN12+}
\label{sec:dataset_s2}

Sentinel-2 is an Earth observation mission operated by the European Space Agency (ESA) as part of the Copernicus programme. The mission includes two satellites, Sentinel-2A and Sentinel-2B, launched in June 2015 and March 2017 from French Guiana. Both satellites orbit Earth at an altitude of 786~km on a sun-synchronous polar orbit, with a 180° phase offset between them. Each satellite has a swath width of 290~km and a revisit time of 10 days. With both satellites operating together, the revisit time is reduced to 5 days at the equator. The mission covers latitudes between 56°S and 84°N. Each satellite is equipped with the MultiSpectral Instrument (MSI), which acquires imagery in 13 spectral bands spanning the visible, near-infrared, and shortwave infrared regions (443--2190~nm) \cite{drusch2012}. Spatial resolution varies across bands: 10~m for the four main visible and near-infrared bands, 20~m for the six red-edge and shortwave infrared bands, and 60~m for the three atmospheric correction bands. The full list of spectral bands is provided in Table~\ref{tab:s2_bands}.

\begin{table}[H]
\small
\centering
\caption{Spectral bands of the Sentinel-2 Multi Spectral Instrument (MSI).} \caption*{Source: \cite{drusch2012}}
\label{tab:s2_bands}
\begin{tabular}{lccc}

\toprule
\textbf{Band} & \textbf{Central Wavelength (nm)} & \textbf{Bandwith (nm)} & \textbf{Resolution (m)} \\
\midrule
B1  & 443  & 20  & 60 \\
B2  & 490  & 65  & 10 \\
B3  & 560  & 35  & 10 \\
B4  & 665  & 30  & 10 \\
B5  & 705  & 15  & 20 \\
B6  & 740  & 15  & 20 \\
B7  & 783  & 20  & 20 \\
B8  & 842  & 115 & 10 \\
B8A & 865  & 20  & 20 \\
B9  & 945  & 20  & 60 \\
B10 & 1375 & 30  & 60 \\
B11 & 1610 & 90  & 20 \\
B12 & 2190 & 180 & 20 \\
\bottomrule
\end{tabular}
\end{table}

CloudSEN12+ \cite{aybar2024} is the Sentinel-2 dataset used in this work. It is an extension of the original CloudSEN12 dataset \cite{aybar2022} and represents the largest publicly available collection of expert-labeled pixels for cloud and cloud shadow detection in Sentinel-2 imagery. All 13 spectral bands are provided at a uniform spatial resolution of 10~m, with the native 20~m and 60~m bands upsampled accordingly. CloudSEN12+ provides global coverage, with pixel-wise annotations for four semantic classes: thick cloud, thin cloud, cloud shadow, and clear. Three annotation quality tiers are available: high-quality, scribble-labeled, and unlabeled.

Although CloudSEN12+ provides global coverage, only patches falling within a selected region of the Middle East and North Africa were used for this study. This geographic restriction was applied to reduce the domain shift between the source and target datasets. The region was selected based on similar land cover characteristics with the VEN$\mu$S dataset, containing vegetated, urban, agricultural, and arid surfaces, as well as water bodies. A particular focus was placed on desert surfaces, which are spectrally challenging for cloud detection due to their high reflectance. A geographically matched training set therefore helps the model learn to distinguish clouds from such surfaces more reliably. As shown in Figure~\ref{fig:cloudsen12_aoi}, the selected patches are predominantly located in coastal and near-coastal zones of the region, which share the closest climatic and land cover characteristics with the VEN$\mu$S target area over Israel.

\begin{figure}[H]
    \centering
    \fbox{\includegraphics[width=0.75\textwidth]{img/cloudsen12_aoi.png}}
    \caption{Geographic distribution of CloudSEN12+ patches within 
    the area of interest. Orange dots indicate patches selected by 
    geographic filtering; green dots indicate patches retained after 
    quality-based filtering during preprocessing.}
    \label{fig:cloudsen12_aoi}
\end{figure}

Only the high-quality annotation subset was used to ensure the highest possible quality of annotations. CloudSEN12+ provides patches in two sizes: $509 \times 509$ pixels (p509) and $2000 \times 2000$ pixels (p2000). Since only four p2000 patches fall within the area of interest, the p2000 subset was excluded. Each p509 location is covered by five scenes with varying cloud cover fractions. The p509 subset was further filtered during preprocessing based on a visual inspection of annotation quality, as described in Section~\ref{sec:preprocessing}. The final number of patches used for training is reported in Table~\ref{tab:dataset_splits}.

\subsection{VEN$\mu$S and the VEN$\mu$S Cloud Masking Dataset}
\label{sec:dataset_venus}

VEN$\mu$S (Vegetation and Environment monitoring on a New Micro-Satellite) is a joint Earth observation mission of the French Centre National d'Études Spatiales (CNES) and the Israeli Space Agency (ISA), launched in August 2017 \cite{salvoldi2022}. The satellite was developed as a demonstration mission for the GMES (Global Monitoring for Environment and Security) programme, the same initiative under which Sentinel-2 was developed, which explains the strong similarity in spectral band configuration between the two sensors in the visible and red-edge regions. The satellite operates in a sun-synchronous orbit at 720~km altitude with 98.28° inclination. It has a swath width of 27.5~km and a revisit time of 2 days. The satellite can be tilted up to 30° along and across track to observe preselected sites, which are always imaged at a constant view azimuth and zenith angle, reducing directional effects. It carries the VSSC (VEN$\mu$S SuperSpectral Camera), which provides imagery in 12 narrow spectral bands covering the visible and near-infrared range (420--910~nm) \cite{salvoldi2022}. The native spatial resolution is 5.3~m. Unlike Sentinel-2, VEN$\mu$S does not include shortwave infrared bands, which are commonly used for cloud detection. The full list of spectral bands is provided in Table~\ref{tab:venus_bands}.

\begin{table}[H]
\small
\centering
\caption{Spectral bands of the VEN$\mu$S SuperSpectral Camera 
(VSSC).}
\caption*{Source: \cite{salvoldi2022}}
\label{tab:venus_bands}
\small
\begin{tabular}{lcc}
\toprule
\textbf{Band} & \textbf{Central Wavelength (nm)} & \textbf{Bandwidth (nm)} \\
\midrule
B1  & 423.9 & 40 \\
B2  & 446.9 & 40 \\
B3  & 491.9 & 40 \\
B4  & 555.0 & 40 \\
B5  & 619.7 & 40 \\
B6  & 619.7 & 40 \\
B7  & 666.2 & 30 \\
B8  & 702.0 & 24 \\
B9  & 741.1 & 16 \\
B10 & 782.2 & 16 \\
B11 & 861.1 & 40 \\
B12 & 908.7 & 20 \\
\bottomrule
\multicolumn{3}{l}{\footnotesize All bands acquired at 5.3~m 
spatial resolution.}\\
\end{tabular}
\end{table}

The VEN$\mu$S cloud masking dataset used in this study was created by Pešek~et~al.~\cite{pesek2022}. The dataset covers three VEN$\mu$S tiles over Israel: W07, S01, and S05, selected to represent the main land cover types present in Israel, including vegetated, urban, agricultural, and arid areas as well as water bodies. Nine scenes were manually labeled, with more than half of the dataset consisting of scenes from the Negev desert tile S05, reflecting the particular challenge of distinguishing clouds from bright arid surfaces. The dataset was created in response to the insufficiency of automatically generated MAJA cloud masks for VEN$\mu$S data, which were found to overestimate cloud cover, particularly over urban and arid regions \cite{pesek2022}. Each scene is pixel-wise annotated with one of three semantic classes: clear, thick cloud, and thin cloud. No cloud shadow class is provided. Tile S01 is a Level~1 top-of-atmosphere product provided at 5~m spatial resolution, while tiles W07 and S05 are Level~2 atmospherically corrected products provided at 10~m \cite{eoportal_venus}. This radiometric inconsistency within the dataset represents a source of variation, which is partially mitigated during preprocessing, as described in Section~\ref{sec:normalization}.

\subsection{Band Selection for Cross-Sensor Transfer}
\label{sec:band_selection}

Although VEN$\mu$S was designed as a demonstration mission for Sentinel-2 and shares a similar spectral configuration in the visible and red-edge regions, it lacks shortwave infrared bands that are present in Sentinel-2. For meaningful cross-sensor transfer, only bands with spectrally equivalent counterparts in both sensors can be used. Seven such band pairs were identified by matching central wavelengths across the two sensors, as listed in Table~\ref{tab:equivalent_bands}. All selected pairs have a central wavelength difference of at most 5~nm. The selection covers the blue, green, red, three red-edge, and narrow near-infrared bands. In the CloudSEN12+ dataset, all bands are already provided at a uniform resolution of 10~m. However, the equivalent Sentinel-2 bands B5--B8A have a native resolution of 20~m and were upsampled to 10~m. 

\begin{table}[H]
\small
\centering
\caption{Spectrally equivalent bands used for cross-sensor 
transfer learning.}
\label{tab:equivalent_bands}
\small
\begin{tabular}{lc|lc|cl}
\toprule
\textbf{VEN$\mu$S} & \textbf{$\lambda$ (nm)} & \textbf{Sentinel-2} & 
\textbf{$\lambda$ (nm)} & \textbf{$\Delta\lambda$ (nm)} & 
\textbf{Description} \\
\midrule
B3  & 491.9 & B2  & 490 & 1.9 & Blue \\
B4  & 555.0 & B3  & 560 & 5.0 & Green \\
B7  & 666.2 & B4  & 665 & 1.2 & Red \\
B8  & 702.0 & B5  & 705 & 3.0 & Red-edge 1 \\
B9  & 741.1 & B6  & 740 & 1.1 & Red-edge 2 \\
B10 & 782.2 & B7  & 783 & 0.8 & Red-edge 3 \\
B11 & 861.1 & B8A & 865 & 3.9 & Narrow NIR \\
\bottomrule
\multicolumn{6}{l}{\footnotesize $\lambda$ denotes the central wavelength.}\\
\multicolumn{6}{l}{\footnotesize $\Delta\lambda$ denotes the 
absolute difference in central wavelength between sensors.}\\
\end{tabular}
\end{table}


\section{Data Preprocessing}
\label{sec:preprocessing}

Before the datasets can be used for model training and evaluation, several preprocessing steps are required. Raw satellite imagery from different sensors differs in spectral configuration, radiometric properties, spatial resolution, and data format. These differences must be minimized to enable meaningful cross-sensor transfer. Both the Sentinel-2 and VEN$\mu$S datasets undergo a set of preprocessing steps: spectral band extraction, annotation class remapping, dataset splitting, tile generation, and normalization. However, not all steps are applied in every experiment as described in Section \ref{chap:experiments}.

In addition to the shared steps, the two datasets require sensor-specific preprocessing. The Sentinel-2 data requires geographic filtering and filtering based on annotation quality, while the VEN$\mu$S data requires handling of nodata values. The sensor-specific preprocessing steps are described in Sections~\ref{sec:s2_preprocessing} and~\ref{sec:venus_preprocessing} respectively, followed by a description of the common dataset generation steps in Section~\ref{sec:dataset_generation}. Normalization strategies are described in Section~\ref{sec:normalization}.

\subsection{Sentinel-2 Preprocessing}
\label{sec:s2_preprocessing}

The initial stage of Sentinel-2 preprocessing involved applying a geographic filter. Since CloudSEN12+ provides global coverage, only those patches that intersected the bounding boxes of the selected countries were kept. To achieve this, the spatial extent of each patch was checked against a collection of country bounding boxes loaded from a GeoPackage file. This step produced a list of patches selected for subsequent processing.

In the second step, the seven spectral bands selected for cross-sensor transfer, as described in Section~\ref{sec:band_selection}, were extracted from each patch. Since CloudSEN12+ stores the annotation mask as a separate band within the same file, the labels were extracted and saved as independent files during this step. The original four-class annotation scheme was remapped to three classes by merging the thin cloud class into the thick cloud class, resulting in cloud, clear, and cloud shadow labels. All 13 spectral bands in CloudSEN12+ are provided at a uniform spatial resolution of 10~m, so no resampling was required.

Following band extraction, a manual quality review was performed on all patches within the area of interest. Each patch was visually inspected and assigned one of three labels: good, bad, or clear. Patches were marked as bad primarily due to annotation quality issues, such as incorrect or incomplete cloud labels. Patches marked as clear contained no cloud coverage. Since cloud-free patches contribute only clear pixels, which are already well represented within partially cloudy patches, retaining all of them would increase training data volume without providing additional value for cloud detection. Only a subset of clear patches was thus kept to maintain a representative sample of cloud-free conditions while avoiding unnecessary duplication. A total of 129 patches were excluded during this review, leaving \textbf{TODO} patches for further processing.

% The remaining patches were then split into training, validation, and test subsets as described in Section~\ref{sec:dataset_generation}. As a final preprocessing step, normalization is applied to the dataset. Since normalization strategy is one of the key experimental variables in this study, the three approaches used are described in detail in Section~\ref{sec:normalization}.

\subsection{VEN$\mu$S Preprocessing}
\label{sec:venus_preprocessing}

Similarly to Sentinel-2 preprocessing, the first step involved extracting the seven spectral bands selected for cross-sensor transfer, as described in Section~\ref{sec:band_selection}. The original three-class annotation scheme was remapped to two classes by merging the thin cloud class into the thick cloud class, resulting in cloud and clear labels. No cloud shadow class is present in the VEN$\mu$S dataset.

A key difference from the Sentinel-2 preprocessing is the presence of nodata pixels in VEN$\mu$S scenes. In the annotation masks, nodata regions were filled with the ignore value of 255, which was later used to exclude these pixels from loss computation during training as described in Section \ref{}.

Unlike the Sentinel-2 dataset, no manual quality review was necessary for the VEN$\mu$S scenes, as all nine scenes were found to be of sufficient annotation quality.

\subsection{Dataset Generation}
\label{sec:dataset_generation}

Following sensor-specific preprocessing, data from both datasets went through a set of modifications in order to create training datasets to be used in the U-Net model. All images were tiled into $512 \times 512$ pixel tiles, which serve as the input for the U-Net model. Although U-Net is a fully convolutional architecture capable of processing inputs of variable spatial dimensions, the repeated pooling operations in the encoder require input dimensions to be divisible by $2^d$, where $d$ is the number of pooling layers. For a standard U-Net with five pooling layers, this means input dimensions must be divisible by 32. Since 509 is not divisible by 32, padding is required. A tile size of $512 \times 512$ pixels was selected as the smallest valid dimension larger than 509. A tile size of $256 \times 256$ pixels was also tested, but initial experiments showed that larger tiles provide better spatial context for accurate identification of cloud edges.

For CloudSEN12+ patches, which have a native size of $509 \times 509$ pixels, reflective padding of 3 pixels was applied to the right and bottom edges to reach the required $512 \times 512$ pixel dimensions. Reflective padding was chosen over zero padding to avoid introducing artificial boundaries that could affect model predictions near tile edges. For VENµS scenes, which have a native size of $2048 \times 2048$ pixels, tiling was performed using a non-overlapping grid.

Both datasets were divided into training, validation, and test subsets. The test set was manually selected prior to dataset generation to represent the full range of cloud types and land cover classes present in each dataset, and was excluded from all subsequent training and validation steps. For both datasets, ten tiles were selected for the test set. The remaining tiles were automatically divided into training and validation subsets using a 75/25 train/validation ratio.

During dataset generation, data augmentation was applied to the training set in the form of rotations by 90°, 180°, and 270°, effectively quadrupling the number of training tiles. Validation and test sets were not augmented. Padded regions and nodata pixels are excluded from loss computation during training using the ignore value of 255 assigned to the annotation masks. The final dataset composition for both datasets is summarized in Table~\ref{tab:dataset_splits}.

\subsection{Normalization Strategies}
\label{sec:normalization}

Normalization is an important preprocessing step in deep learning, as it reduces the influence of varying radiometric scales across sensors and acquisition conditions, helping the model converge faster and generalize better. In standard deep learning workflows, normalization statistics are derived from the training dataset and applied to all inputs on a per-band basis. However, this approach may not be well-suited for cross-sensor transfer learning, where inputs from different sensors or processing levels can have different value distributions, with values from the target sensor potentially falling outside the range seen during training \cite{wright2025}. In this study, normalization strategy is one of the key experimental variables, and four strategies are investigated to assess their effect on cross-sensor transfer learning performance.

The first strategy uses \textbf{raw} data without any normalization. The original reflectance values are passed directly to the model, preserving the full radiometric information but exposing the model to the full cross-sensor radiometric differences during transfer learning.

The remaining three strategies are based on Z-score normalization, which transforms each band independently according to:

\begin{equation}
\hat{x}_b = \frac{x_b - \mu_b}{\sigma_b}
\label{eq:zscore}
\end{equation}

where $x_b$ is the original pixel value in band $b$, $\mu_b$ is the mean, and $\sigma_b$ is the standard deviation. The three strategies differ in how $\mu_b$ and $\sigma_b$ are computed.

In \textbf{fixed Z-score normalization with Sentinel-2 statistics}, $\mu_b$ and $\sigma_b$ are computed from the Sentinel-2 training set, excluding nodata pixels. The same statistics are then applied to both the Sentinel-2 and VEN$\mu$S data. This approach maps both datasets into the same radiometric space defined by the Sentinel-2 training distribution, which is particularly relevant for the transductive transfer setting where the model is trained on Sentinel-2 data and evaluated on VEN$\mu$S data without any target domain adaptation.

In \textbf{fixed Z-score normalization with VEN$\mu$S statistics}, $\mu_b$ and $\sigma_b$ are computed from the VEN$\mu$S training split only, excluding nodata pixels, and applied to VEN$\mu$S data. This is the natural choice in the inductive and from-scratch settings. However, as discussed in Section~\ref{sec:dataset_venus}, the VEN$\mu$S training set contains a mixture of Level~1 top-of-atmosphere and Level~2 atmospherically corrected tiles, which differ significantly in their radiometric distributions. The computed statistics therefore represent a mixture of two distinct radiometric regimes, which may limit the effectiveness of this strategy.

In \textbf{dynamic Z-score normalization}, proposed by Wright et al.\ \cite{wright2025}, $\mu_b$ and $\sigma_b$ are computed independently for each individual tile from its own valid pixels, excluding nodata values. Unlike fixed normalization strategies that use dataset-wide statistics, this approach normalizes each tile independently to have zero mean and unit standard deviation per band, regardless of the sensor, processing level, or acquisition conditions. This makes it particularly suitable for cross-sensor transfer learning as it effectively eliminates radiometric differences between sensors without requiring any prior knowledge of the target sensor's statistics. Nodata pixels are excluded from the statistics computation and set to zero after normalization. The four normalization strategies are summarized in Table~\ref{tab:normalization}.

\begin{table}[H]
\centering
\caption{Summary of normalization strategies used in 
this study.}
\label{tab:normalization}
\small
\begin{tabular}{llll}
\toprule
\textbf{Strategy} & \textbf{Statistics source} & 
\textbf{Applied to} & \textbf{Scope} \\
\midrule
Raw & — & S2, VEN$\mu$S & — \\
Fixed Z-score (S2) & S2 training set & S2, VEN$\mu$S & 
Global \\
Fixed Z-score (VEN$\mu$S) & VEN$\mu$S training set & 
VEN$\mu$S & Global \\
Dynamic Z-score & Per tile & S2, VEN$\mu$S & Per tile \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Preprocessing Pipeline}
\label{sec:pipeline}

The preprocessing steps described in this chapter were implemented as a modular Python pipeline developed as part of this thesis and publicly available on GitHub. The pipeline consists of six scripts, each implementing one preprocessing step, and a sensor-specific JSON configuration file that specifies dataset paths, band indices, class remapping, and normalization parameters. The manual quality review step is implemented as an interactive Jupyter notebook, allowing visual inspection of each patch and assignment of quality labels. The full pipeline structure is illustrated in Figure~\ref{fig:pipeline}.

The dataset generation step uses a function from cnn-lib, an open-source Python library for semantic segmentation of remote sensing imagery using CNNs, developed by the thesis supervisor and further extended during this thesis. In addition to dataset generation, cnn-lib provides functionality for model training, transfer learning, and inference, and is used throughout all subsequent experimental steps described in this work. The padding  and transfer learning implementation were developed as extensions to cnn-lib as part of this thesis. The preprocessing pipeline developed as part of this thesis is publicly available on GitHub.\footnote{\url{https://github.com/ctu-geoforall-lab-projects/dp-klimes-2026}} The cnn-lib is also publicly available on GitHub.\footnote{\url{https://github.com/ctu-geoforall-lab/cnn-lib}}

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.55cm,
    every node/.style={scale=0.85},
    sbox/.style={
        draw, rounded corners=3pt, thick,
        minimum width=3.8cm, minimum height=0.9cm,
        align=center, font=\small, fill=guiblue!15
    },
    vbox/.style={
        draw, rounded corners=3pt, thick,
        minimum width=3.8cm, minimum height=0.9cm,
        align=center, font=\small, fill=guigreen!15
    },
    databox/.style={
        draw, dashed, rounded corners=3pt,
        minimum width=3.4cm, minimum height=0.7cm,
        align=center, font=\small\itshape
    },
    cfgbox/.style={
        draw, rounded corners=3pt,
        minimum width=3.4cm, minimum height=0.7cm,
        align=center, font=\small\itshape,
        fill=guigray!20
    },
    arr/.style={-{Stealth[length=2.5mm]}, thick}
]

% ==================== S2 COLUMN ====================
\node[databox] (s2raw) at (-3.5, 0) 
    {Sentinel-2 raw patches};

\node[cfgbox, below=0.4cm of s2raw] (s2cfg) 
    {Sentinel-2 configuration\\
    {\footnotesize \texttt{s2\_config.json}}};

\node[sbox, below=0.4cm of s2cfg] (selrast) 
    {Geographic filtering\\
    {\footnotesize \texttt{select\_rasters.py}}};

\node[sbox, below=of selrast] (s2extract) 
    {Band extraction, \\remapping\\
    {\footnotesize \texttt{extract\_bands.py}}};

\node[sbox, below=of s2extract] (review) 
    {Manual quality review\\
    {\footnotesize good / bad / clear}};

\node[sbox, below=of review] (filterrev) 
    {Quality filtering\\
    {\footnotesize \texttt{filter\_review.py}}};

\node[sbox, below=of filterrev] (s2gen) 
    {Tiling, padding, splitting\\
    {\footnotesize \texttt{generate\_dataset.py}}};

\node[sbox, below=of s2gen] (s2norm) 
    {\textbf{Normalization}\\
    {\footnotesize \texttt{normalize.py}}};

\node[databox, below=0.4cm of s2norm] (s2out) 
    {Sentinel-2 training dataset};

% ==================== VENUS COLUMN ====================
\node[databox] (venusraw) at (3.5, 0) 
    {VEN$\mu$S raw scenes};

\node[cfgbox, below=0.4cm of venusraw] (venuscfg) 
    {VEN$\mu$S configuration\\
    {\footnotesize \texttt{venus\_config.json}}};

\node[vbox, below=0.4cm of venuscfg] (venusextract) 
    {Band extraction, \\Remapping, \\nodata handling\\
    {\footnotesize \texttt{extract\_bands.py}}};

\node[vbox, below=of venusextract] (venusgen) 
    {Tiling, splitting\\
    {\footnotesize \texttt{generate\_dataset.py}}};

\node[vbox, below=of venusgen] (venusnorm) 
    {Normalization\\
    {\footnotesize \texttt{normalize.py}}};

\node[databox, below=0.4cm of venusnorm] (venusout) 
    {VEN$\mu$S training dataset};

% ==================== S2 ARROWS ====================
\draw[arr] (s2raw) -- (s2cfg);
\draw[arr] (s2cfg) -- (selrast);
\draw[arr] (selrast) -- (s2extract);
\draw[arr] (s2extract) -- (review);
\draw[arr] (review) -- (filterrev);
\draw[arr] (filterrev) -- (s2gen);
\draw[arr] (s2gen) -- (s2norm);
\draw[arr] (s2norm) -- (s2out);

% ==================== VENUS ARROWS ====================
\draw[arr] (venusraw) -- (venuscfg);
\draw[arr] (venuscfg) -- (venusextract);
\draw[arr] (venusextract) -- (venusgen);
\draw[arr] (venusgen) -- (venusnorm);
\draw[arr] (venusnorm) -- (venusout);

% ==================== LABELS ====================
\node[font=\small\bfseries, guiblue!70!black] at (-3.5, 0.7) 
    {Sentinel-2};
\node[font=\small\bfseries, guigreen!60!black] at (3.5, 0.7) 
    {VEN$\mu$S};

\end{tikzpicture}
\caption{Structure of the preprocessing pipeline.}
\label{fig:pipeline}
\end{figure}

\section{Model and Training}

\section{Evaluation}